import HospitalPortal from '@/components/pages/hospital-portal';

export const metadata = {
  title: 'Hospital Dashboard — OmniMed AI',
  description: 'AI-powered clinical analysis dashboard for hospitals. Department-level scan analysis with structured reports.',
};

export default function HospitalsPage() {
  return <HospitalPortal />;
}
